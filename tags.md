tags:
1. tag tab should killed and it's functionality spread across UI
2. bottom tag bar should be limited in size - if more tags than can fit exist it should show a scroll (left right)
3. right clicking a tag should have a delete button - deleting must show a warning confirmation - this tag will not be avialable for use and removed from tagged items
4. the tags bar should have a little + button at the right "create new tag"
5. right clicking a tag should have a rename option
6. right lciking a tag shouldf have a change color option

settings:
1. should have a load on system start-up default on which affects system tray loading
2. settings should be saved immidietly as they are changed no need for that actions / save button
3. instead there should be a restore system defaults which loads default settings for everything
4. there should be a limit to storage use, options are unlimited, limit to (input) x items, limit to size in MB
5. if storage limit is set there should be two options - when limit reached delete oldest or stop recording new items and show a warning
6. the above warning would need to be shown in a notification for this we need a notification history bar
7. the notification history bar will be a new icon in the header near about
8. about should only show the logo and subtitle the rest in the about is a little wierd and off taste
9. different sections of the settings should be foldable when title clicked with a cheveron indicating it can be open
10. the shortcut keyboard should be open by default and the rest closed