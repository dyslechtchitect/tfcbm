# TFCBM Flatpak Python Dependencies
# Only packages NOT provided by org.gnome.Platform runtime

# Configuration parsing
PyYAML==6.0.2

# Image processing for thumbnails
Pillow==11.0.0

# WebSocket server for clipboard monitoring
websockets==15.0.1

# NOTE: PyGObject is provided by org.gnome.Platform, do NOT install via pip
# NOTE: pytest is for development only, not needed in flatpak
