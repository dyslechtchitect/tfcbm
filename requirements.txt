# TFCBM - The Friendly Clipboard Manager
# Python dependencies for local development

# Production dependencies
# GTK4 (UI framework) - includes GdkPixbuf for image processing
PyGObject>=3.42.0

# Development dependencies
pytest>=8.4.2

# System dependencies (install via system package manager, not pip)
# xdotool - Required for keyboard shortcut auto-paste feature
#   Fedora: sudo dnf install xdotool
#   Debian/Ubuntu: sudo apt install xdotool
#   Alternative (Wayland): ydotool
